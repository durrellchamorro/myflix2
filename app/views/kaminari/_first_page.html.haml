- if current_page.first?
  %li.disabled
    = link_to raw(t 'views.pagination.first'), url, :rel => 'next', :remote => remote
- else
  %li
    = link_to raw(t 'views.pagination.first'), url, :rel => 'next', :remote => remote
