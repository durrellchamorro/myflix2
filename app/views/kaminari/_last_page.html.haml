- if current_page.last?
  %li.disabled
    = link_to raw(t 'views.pagination.last'), url, :rel => 'next', :remote => remote
- else
  %li
    = link_to raw(t 'views.pagination.last'), url, :rel => 'next', :remote => remote
